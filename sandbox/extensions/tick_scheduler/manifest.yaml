id: tick_scheduler
name: Tick Scheduler
version: "1.0.0"

entrypoint: main:TickSchedulerExtension

description: |
  Demo scheduler. Fires every minute and sends a "tick at HH:MM:SS" notification to the user.
  Used to verify SchedulerProvider and the notify path to CLI.

depends_on: []
secrets: []
config: {}
enabled: false
