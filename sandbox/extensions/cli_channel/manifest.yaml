id: cli_channel
name: CLI Channel
version: "1.0.0"

entrypoint: main:CliChannelExtension

description: |
  CLI channel. User types messages in the terminal,
  agent responds in the same terminal.

depends_on: []
secrets: []
config: {}
enabled: true
