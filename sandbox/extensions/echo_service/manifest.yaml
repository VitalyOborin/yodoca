id: echo_service
name: Echo Service
version: "1.0.0"

description: Background service that logs a tick every 10 seconds. Verifies ServiceProvider run_background is run as asyncio.Task and handles CancelledError on shutdown.

entrypoint: main:EchoServiceExtension

natural_language_description: |
  Demo background service. Logs "echo_service tick #N" every 10 seconds.
  Used to verify ServiceProvider lifecycle and non-blocking task.

depends_on: []
secrets: []
config: {}
enabled: false
